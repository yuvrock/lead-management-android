<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_camera"
            android:icon="@drawable/ic_menu_home"
            android:title="@string/home_fragment" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/ic_menu_contacts"
            android:title="@string/contacts_fragment" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/ic_menu_group"
            android:title="@string/groups_fragment" />
        <item
            android:id="@+id/nav_manage"
            android:icon="@drawable/ic_menu_settings"
            android:title="@string/settings_fragment" />
    </group>

    <item android:title="@string/other_options">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_info"
                android:title="@string/about_us_fragment" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_policy"
                android:title="@string/privacy_policy_fragment" />
        </menu>
    </item>

</menu>
